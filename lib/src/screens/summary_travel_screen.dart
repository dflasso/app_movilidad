import 'package:flutter/widgets.dart';

class SummaryTravelScreen extends StatelessWidget {
  const SummaryTravelScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Text("resumen del viaje"),
    );
  }
}
